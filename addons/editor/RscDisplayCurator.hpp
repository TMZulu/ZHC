class RscText;
class RscEdit;
class RscLine;
class RscFrame;
class RscPicture;
class RscButtonSearch;
class RscControlsGroupNoScrollbars;

class RscTree {
    class ScrollBar;
};

class EGVAR(common,RscCheckbox);

class RscDisplayCurator {
    class Controls {
		class ZHCIcons {
			x = "((safeZoneX + safezoneW)/2) - ((16 *((	(safezoneW / safezoneH) min 1.2) / 40))/2)";
            idc = 16810;
            y = "1.5 * ((	((safezoneW / safezoneH) min 1.2) / 1.2) / 25) + (safezoneY)";
			w = GUI_BOX_W(16);
            h = "1.2 *((	((safezoneW / safezoneH) min 1.2) / 1.2) / 25)";
			
			class controls
            {

			};
		};
	};
};